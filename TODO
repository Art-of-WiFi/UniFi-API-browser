TODO:
1)  Make the output format selectable like with the siteid, so that
    after selection we can switch between the two lines below for
    different output presentation.

    Options are PHP array vs json:
    - print_r ($data);
    - echo json_encode($data, JSON_PRETTY_PRINT);